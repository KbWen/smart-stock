# Runtime dependencies — pinned to the verified working set so results are
# reproducible (these affect model output / API behavior). Bump deliberately,
# re-run the test suite, then update the pin.
fastapi==0.129.2
uvicorn[standard]==0.41.0
slowapi==0.1.9
pandas==3.0.1
numpy==2.4.2
yfinance==1.2.0
twstock==1.4.0
scikit-learn==1.8.0
joblib==1.5.3
requests==2.32.5

# Dev / test tooling — intentionally left unpinned (does not affect app output).
# Note: avoid pytest 8.4.1 (CVE-2025-71176); any recent pytest works.
pytest
httpx
pytest-mock
