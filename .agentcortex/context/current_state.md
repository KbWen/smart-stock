# Project Current State (vNext)

- **Project Intent**: Taiwan stock analysis platform with AI-powered buy signal prediction using ensemble ML (Gradient Boosting + Random Forest + MLP), technical indicators, and V4 sniper scoring system.
- **Project Name**: smart-stock
- **Core Guardrails**:
  - Correctness first: No claim of completion without evidence.
  - Small & reversible: Prioritize small, reversible changes; avoid unauthorized refactoring.
  - Document-first: Core logic or structural changes require a Spec/ADR first.
  - Handoff gate: Non-`tiny-fix` tasks must produce a traceable handoff summary.
- **System Map**:
  - Global SSoT: `.agentcortex/context/current_state.md`
  - Task Isolation: `.agentcortex/context/work/<worklog-key>.md`
  - Active Work Log Path: derive <worklog-key> from the raw branch name using filesystem-safe normalization before any gate checks.
  - Workflows & Policies: `.agent/workflows/*.md`, `.agent/rules/*.md`
- **Last Updated**: 2026-07-26 (Tiers 1-2 repo hygiene shipped; Tier 3 pending)
- **Last Verified**: 2026-07-02 (brain updated to Agentic OS v1.8.7)
- **Update Sequence**: 2 (counter introduced at v1.8.7; maintained by /ship going forward)
- **ADR Index**:
  - docs/adr/ADR-001-vnext-self-managed-architecture.md: vNext self-managed architecture · applies_to: .agentcortex/
  - docs/adr/ADR-002-build-system-modernization.md: [Proposed] pyproject + dep split + lockfile (from 2026-07-19 audit) · applies_to: requirements.txt, pyproject.toml, backend/, core/
  - docs/adr/ADR-003-data-module-decomposition.md: [Proposed] core/data.py god-module split (defer unless triggered) · applies_to: core/data.py
  - docs/adr/ADR-004-model-rotation-sort-key.md: [Proposed] rotation net-vs-gross profit factor · applies_to: core/ai/common.py, core/ai/trainer.py, backend/manage_models.py
- **Active Backlog**: `docs/specs/_product-backlog.md` (**Honest Research Workbench** epic, opened 2026-07-02: #1 Strategy Lab ✅ **Shipped**, #2 Transparency Panel ✅ **Shipped**, #3 Explainable Screening ✅ **Shipped**, #4 Novice Entry ✅ **Shipped**, #5 Reproducible Reference Layer ✅ **Shipped**, #6 CSRF-parity [review-finding] ✅ **Shipped** — **ALL SHIPPED, epic complete 2026-07-02**. Differentiator = transparency, not prediction accuracy; progressive disclosure for novice+expert. Prior Directly-Usable v1 backlog [all 6 shipped] archived to `docs/specs/_product-backlog-directly-usable-v1-2026-06-20.md`)
  - When a multi-feature product spec is decomposed, the backlog path is recorded here (e.g., `docs/specs/_product-backlog.md`). Bootstrap reads this to detect ongoing product work.
- **Spec Index**:
  - `[api-perf] docs/specs/api-refactor-perf.md [Frozen] — ✅ ALL 5 ACs done (batch benchmark test added 2026-03-17)`
  - `[backend] docs/specs/backend-refactor-modular.md [Frozen] — ✅ ALL 8 ACs done (schema parity tests added 2026-03-17)`
  - `[frontend-api] docs/specs/frontend-api-opt.md [Frozen] — ✅ ALL 4 ACs done (E2E real render benchmark added 2026-03-18)`
  - `[frontend-test] docs/specs/frontend-testing.md [Frozen] — ✅ ALL 6 ACs done (82.7% coverage confirmed 2026-03-22) [Updated: 2026-03-22]`
  - `[cache] docs/specs/smart-stock-cache.md [Frozen] — ✅ ALL 5 ACs done (GlassCard+Button extracted 2026-03-17)`
  - `[ml-rotation] docs/specs/ml-model-rotation.md [Frozen] — ✅ ALL 4 ACs done (profit_factor rotation + None guard + MAX_SAVED_MODELS + active model protection 2026-03-18)`
  - `[fe-lazy] docs/specs/frontend-lazy-loading.md [Frozen] — ✅ ALL 4 ACs done (already implemented: App.tsx full-page lazy, MarketRisk inner Suspense, recharts in async chunks 2026-03-18)`
  - `[meta-norm] docs/specs/meta-normalization.md [Frozen] — ✅ ALL 3 ACs done (single-pass normalization, route passes requested_pairs to service 2026-03-18)`
  - `[predictor-ts] docs/specs/predictor-thread-safety.md [Frozen] — ✅ ALL 3 ACs done (threading.Lock + OrderedDict LRU cache maxsize=3 2026-03-18)`
  - `[meta-tests] docs/specs/meta-service-tests.md [Frozen] — ✅ ALL 5 ACs done (23 new tests: _to_bool, signals, safe defaults, updated_at fallback 2026-03-18)`
  - `[api-security] docs/specs/api-security-hardening.md [Frozen] — ✅ ALL 12 ACs done (path traversal, info-leak, rate limits, ticker validation, bare excepts, SHA256 integrity 2026-03-19)`
  - `[audit-doc-test] docs/specs/audit-doc-test-supplement.md [Frozen] — ✅ ALL 9 ACs done (HMAC tests 9/9, model-signing.md, rate-limiting.md 2026-03-21)`
  - `[visual-upgrade-p1] docs/specs/visual-upgrade-phase1.md [Frozen] — ✅ ALL 6 ACs done (history endpoint + PriceSignalChart + count-up animation 2026-03-27)`
  - `[visual-upgrade-p2] docs/specs/visual-upgrade-phase2.md [Frozen] — ✅ ALL 7 ACs done (sparkline endpoint + SparklineChart + CandidateRow integration 2026-03-28)`
  - `[onboarding] docs/specs/onboarding-optimization.md [Frozen] — ✅ ALL 4 ACs done (dev startup and limit sync optimized 2026-06-01)`
  - `[frontend-ai] docs/specs/frontend-ai-optimization.md [Frozen] — ✅ ALL 4 ACs done (SVG sparklines, animations, glassmorphism, AI verification 2026-06-01)`
  - `[core-ai-opt] docs/specs/ai-pipeline-otc-optimization.md [Frozen] — ✅ ALL 5 ACs done + US/Crypto suffix lookup fix 2026-06-02`
  - `[backtest-perf] docs/specs/backtest-and-performance-opt.md [Frozen] — ✅ ALL 5 ACs done (batch query + transaction cost sliders + sharpe & drawdown metrics + CandidateRow optimization + dynamic strategy parameters + production build 2026-06-02)`
  - `[honest-fe] docs/specs/frontend-honest-data-states.md [Frozen] — ✅ ALL 6 ACs done (removed mockData fallback; nullable useCachedApi; honest loading/no-data/error states 2026-06-13)`
  - `[honest-be] docs/specs/backend-failure-state-honesty.md [Frozen] — ✅ ALL 6 ACs done (predict_prob None on failure; ai_prob NULL not fake 0.0; to_ai_percent; frontend N/A 2026-06-13)`
  - `[model-state] docs/specs/ui-model-state-disclosure.md [Frozen] — ✅ ALL 4 ACs done (get_model_health + market_status.model_health + ModelHealthBanner 2026-06-13)`
  - `[backtest-labels] docs/specs/backtest-metric-label-honesty.md [Frozen] — ✅ ALL 4 ACs done (profit_factor None not 9999; unannualized Sharpe relabel; dynamic tooltips) [EXTENDS backtest-perf] 2026-06-13`
  - `[docs-sync] docs/specs/docs-reality-sync.md [Frozen] — ✅ ALL 6 ACs done (API_CONTRACT/README/ARCHITECTURE/TESTING/db-operations corrected 2026-06-13)`
  - `[fe-ci] docs/specs/frontend-ci.md [Frozen] — ✅ ALL 3 ACs done (Frontend CI: vitest + production build) [EXTENDS frontend-test] 2026-06-13`
  - `[data-universe] docs/specs/listed-otc-data-completeness.md [Frozen] — ✅ ALL 6 ACs done (live TWSE/TPEX universe sourcing + ETF inclusion + market/kind tags + history-coverage report/backfill; twstock demoted to fallback) 2026-06-13`
  - `[ml-labels] docs/specs/ml-label-volatility-scaling.md [Frozen] — ✅ ALL 6 ACs done (ATR volatility-scaled triple-barrier labels fix degenerate-model root cause; LABEL_MODE atr/fixed toggle; label_analysis evidence tool. Validated label rebalancing 91/5/4→59.5/13.7/26.8; full-universe OOS NOT claimed — retrain pending) 2026-06-13`
  - `[onboarding-qs] docs/specs/onboarding-quickstart.md [Frozen] — ✅ ALL 6 ACs done (offline demo fixture data/demo/demo_prices.csv + idempotent scripts/seed_demo.py + quickstart.sh/.ps1 + README; recalc gating fix so no-model fresh clone gets technical scores w/ honest NULL AI) 2026-06-13`
  - `[ml-oos-eval] docs/specs/ml-label-oos-evaluation.md [Reference] — OOS eval of atr vs fixed on 92-ticker expanded data (scripts/eval_label_modes.py). HONEST: degeneracy did NOT reproduce at scale (small-data artifact); atr modestly better on StrongBuy (P .339→.363, R .424→.518); absolute precision still low; ~5% universe, not full. 2026-06-13`
  - `[onboarding-launch] docs/specs/onboarding-single-command-launch.md [Frozen] — ✅ ALL 6 ACs done (quickstart builds UI; backend serves built SPA single-port :8000 + react-router deep-link fallback excl. /api,/assets,/static; .ps1/.bat/.sh parity incl. new start.ps1; README reorder; honesty-first unchanged) [EXTENDS onboarding-quickstart] 2026-06-20`
  - `[honest-ux] docs/specs/honest-first-run-ux.md [Frozen] — ✅ ALL 6 ACs done (繁中 nav/states; demo "示範模式" badge from model_health; AI N/A reframed + Tooltip; sync confirm; backend AI Analyst localized; +4 Dashboard tests; design-gate screenshot-verified) [EXTENDS frontend-honest-data-states + ui-model-state-disclosure] 2026-06-20`
  - `[accel-sync] docs/specs/accelerated-universe-sync.md [Frozen] — ✅ ALL 6 ACs done (core/bulk_history.py TWSE MI_INDEX + TPEX daily-close per-day bulk parsers + backfill_bulk; scripts/fast_backfill.py; full universe ~2 calls/day; yfinance untouched fallback; raw prices documented; honest save-count) [EXTENDS listed-otc-data-completeness] 2026-06-20`
  - `[real-ai] docs/specs/real-ai-first-run.md [Frozen] — ✅ ALL 6 ACs done (scripts/setup_real_ai.py opt-in backfill→gate→train→recalc; honest gating skips degenerate train, AI stays N/A; ~4y default; weak model disclosed by model_health; chain payoff proven on real OOS data) [EXTENDS onboarding-quickstart + accelerated-universe-sync] 2026-06-20`
  - `[docker-seed] docs/specs/docker-self-seed.md [Frozen] — ✅ ALL 5 ACs done (Dockerfile COPY scripts+data/demo; seed-then-serve entrypoint → populated dashboard not empty; .dockerignore excludes dev DB (root+data/) / models / node_modules, keeps demo; AI stays N/A; guard test; review caught+fixed root-DB bake) [EXTENDS onboarding-quickstart] 2026-06-20`
  - `[strategy-lab] docs/specs/strategy-lab.md [Frozen] — ✅ ALL 7 ACs done (saveable/comparable backtest strategies: strategies table + Pydantic StrategyParams CRUD + throttled compare API w/ honest error slots + Strategy Lab UI w/ progressive disclosure, suggested-default, no-winner honesty; 3-agent pre-impl panel + independent review READY, findings fixed) [EXTENDS backtest-and-performance-opt] 2026-07-02`
  - `[transparency-panel] docs/specs/transparency-panel.md [Frozen] — ✅ ALL 5 ACs done (GET /api/transparency aggregates report_history_coverage + get_history_context + get_model_health + oos_metrics; TTL-cached + rate-limited, counts-only, honest nulls; /transparency page w/ progressive disclosure, OOS "not a guarantee" framing, class-distribution disclosure; independent review + tenth-man READY, real-schema-pin test added; live-verified honest degraded-model disclosure) 2026-07-02`
  - `[explainable-screening] docs/specs/explainable-screening.md [Frozen] — ✅ ALL 5 ACs done (stock detail += guarded model_health both paths; ScoreBreakdown qualifies the AI number by model_health + surfaces fired signals as plain-language chips + sub-score captions; honest "not a validated per-signal win-rate" note, per-signal hit-rate deferred not faked; independent review + tenth-man READY, get_model_health-failure resilience guard + both-path tests; live-verified) [EXTENDS ui-model-state-disclosure] 2026-07-02`
  - `[novice-entry] docs/specs/novice-entry.md [Frozen] — ✅ ALL 5 ACs done (dismissible/localStorage-remembered GettingStarted panel on Dashboard: honest positioning "not investment advice/not a tipster" + expectations + guided react-router links + demo-mode note; frontend-only; independent review + tenth-man READY) 2026-07-02`
  - `[strategies-csrf-parity] docs/specs/strategies-csrf-parity.md [Frozen] — ✅ ALL 3 ACs done (quick-win/review-finding: X-Requested-With 403 gate on POST/PUT/DELETE /api/strategies, parity w/ smart_scan; GET unaffected; frontend mutateJson already sends it; falsifiable 403 test; independent review + tenth-man READY) [EXTENDS strategy-lab] 2026-07-02`
  - `[reproducible-reference] docs/specs/reproducible-reference.md [Frozen] — ✅ ALL 5 ACs done (scripts/reproduce_pipeline.py injectable orchestrator: map default w/ zero side effects/heavy imports + honest --run auto-running only robust stages, label/train/eval reported auto_ran:False not faked, stage failures recorded not raised; docs/REPRODUCE.md walkthrough w/ degenerate-model caveat everywhere; all doc refs verified real; independent review + tenth-man READY) 2026-07-02`
  - When reading specs: only open files tagged with the current task's module.
- **Canonical Commands**:
  - `/spec-intake`: Import external specs (from other LLMs, documents, or natural language). Handles large product specs via decomposition. Runs before `/bootstrap`.
  - `/bootstrap`: Task initialization & classification freeze.
  - `/plan`: Define target files, steps, risks, and rollback.
  - `/implement`: Execute implementation only when `IMPLEMENTABLE`.
  - `/review`: Check AC alignment & scope creep.
  - `/test`: Report test coverage via Test Skeleton.
  - `/handoff`: Output resumable state summary (mandatory for non-tiny-fix).
  - `/decide`: Record key decisions with reasoning to prevent cross-session re-derivation.
  - `/test-classify`: Auto-select test depth and evidence format based on task classification.
  - `/ship`: Consolidate evidence and update/archive state.
  - `ask-openrouter`: [OPTIONAL] External model delegation (natural language or `/or-*` commands). See `.agent/workflows/ask-openrouter.md`.
  - `codex-cli`: [OPTIONAL] Codex CLI delegation. See `.agent/workflows/codex-cli.md`.
- **References**:
  - `AGENTS.md`
  - `.agent/rules/engineering_guardrails.md`
  - `.agent/rules/state_machine.md`
  - `.agentcortex/docs/CODEX_PLATFORM_GUIDE.md`
  - `.agentcortex/docs/guides/token-governance.md` *(manual-only)*
  - `.agentcortex/docs/guides/context-budget.md` *(manual-only)*

> [!NOTE]
> This file is the Single Source of Truth for global project context only.
> Do not store per-task progress here; write progress to `.agentcortex/context/work/<worklog-key>.md`.

## Global Lessons (AI Error Pattern Registry)
>
> 3-5 high-value patterns max. Reviewed during /bootstrap.

- [Build Mode Strictness]: `tsc --noEmit` may pass while `npm run build` (`tsc -b`) fails on stricter generic type constraints (e.g., Recharts Formatter). Always run full production build as final validation before shipping.
- [Format Safety]: Do not copy line numbers from view tools; they break file edits.
- [E2E Perf Testing]: E2E performance tests must run against production build (`vite preview`), not dev server. React StrictMode double-invocation in dev inflates timing 3-6×, masking true render performance.
- [Work Log Lag]: Evidence must be written to Work Log during each phase (implement/review/test), not accumulated for ship. Stale logs block the ship gate and require recovery before proceeding.
- [Rotation Conflict]: When trainer.py uses timestamp-based rotation and manage_models.py uses quality-based pruning independently, a bad new model can displace a good old one before prune runs. Unify via a shared constant and quality-first sort key.
- [Sidecar Parity]: Whenever a `.pkl` model file is deleted (rotation or prune), also delete matching `.sha256`/`.sig` sidecars. trainer.py rotation and manage_models.py:cmd_delete must stay in sync on this.
- [Stacked-PR Merge]: When merging stacked PRs, retarget each dependent PR's base to mainline BEFORE merging+deleting its base branch. `gh pr merge --delete-branch` on the base auto-CLOSES the dependent PR and GitHub will NOT reopen it once the base branch is gone (cost a #23→#25 recreation). Either retarget-first, or omit `--delete-branch` until dependents are retargeted.
- [Claim vs Real Target]: Verify "it works" against the REAL target scenario, not the local dev state. The onboarding "populated dashboard" claim was false for a fresh clone (gitignored storage.db/model) until a recalc model-gating bug was fixed — local dev had a model so it masked the gap.

## Ship History

### Ship-quickwin-repo-hygiene-tier2-2026-07-26
- Quick-win shipped: Tier 2 repo hygiene (batch 2 of 3). No application behavior change. (1) Deleted the whole orphaned `tools/` directory (4 files) — every one has a canonical counterpart under `.agentcortex/tools/`, which `deploy.sh:739` maintains as `_runtime_tools` and the manifest records; `tools/` is absent from the manifest, never deployed, and referenced by nothing in the repo, CI, Docker, or launch scripts. Leftover from the 2026-06-13 AgentCortex v5.3.0 -> agentic-os v1.5.3 migration. (2) Root `CONTRIBUTING.md` was titled "Contributing to **AgentCortex**" and pointed at the pre-vNext `docs/context/current_state.md` — the same wrong-project leak class as the CITATION.cff / README_zh-TW.md files fixed in #47; replaced with the correct Smart Stock guide that had been sitting unreferenced in `docs/project_meta/`, with its two bare `TESTING.md`/`CODE_STYLE.md` links repointed now that the file sits at the root. (3) `_product-backlog-honesty-first-2026-06-13.md` was still `status: living` despite its own `last_updated` recording all 6 items shipped via PR #20 — set to `archived`, matching its two sibling snapshots. **Scope was corrected 5 items -> 3 during bootstrap**: `scripts/daily_run.sh` turned out to be a deliberate backward-compat wrapper, and `codex/` vs `.codex/` are both framework-managed with distinct roles (`codex/rules/default.rules` is validate's `ACTIVE_CODEX_RULES`), so both were dropped rather than "cleaned up". Conversely `tools/` proved larger than reported — the whole directory, not just two files. Out of scope: Tier 3 (17 drifted docs between `docs/` and `.agentcortex/docs/` + the `.gitignore` EN/zh-TW asymmetry), likely ADR-worthy.
- Tests: Pass (288 passed / 1 deselected, unchanged — this batch adds no tests and touches no code; Tier 1's `tests/test_repo_hygiene.py` guards still 5/5; `validate.sh` 92 PASS / 9 WARN / 0 FAIL against a stash-clean baseline, the single delta being governed-write lint scanning 148 -> 145 files)

### Ship-hotfix-repo-hygiene-tier1-2026-07-26
- Hotfix shipped: Tier 1 repo hygiene (batch 1 of 3). No application behavior change. (1) Renamed two archived Work Logs whose filenames carried mojibake from a shell-quoting accident (`frontend-testing.md埋點不再。`, `optimize-frontend-api.md埋點`) — content hash-identical apart from the two remediations below. (2) Removed root `deploy_brain.{sh,ps1,cmd}`: unmanaged orphans that `deploy.sh:839-841` never deploys, `validate.sh:34-36` never resolves to, and `.agentcortex-manifest:208-210` never records. They were also strictly weaker than the surviving `installers/` copies — no cache-origin verification, no `--source` peek, no half-delete guard, and a pre-migration source URL in the error hint — so deleting them removed a path that pulled and exec'd a framework source without checking its origin. (3) Untracked + gitignored `stock_list_cache.json` and `tw_stocks_analysis_refined.csv` (runtime artifacts written by `core/config.py:58` / `fetch_stocks.py:114`); verified against a simulated fresh clone, where the cache-less path resolves 2128 tickers live vs the 1819 in the cache last committed 2026-06-01. Classified `hotfix` NOT for urgency but because the Supply-Chain/Provenance Escalation in `state_machine.md` forces that minimum whenever installer source-selection logic is touched, which buys the REVIEWED + TESTED gates. **Honest finding**: renaming the two archive files made them visible to every `*.md` validator for the first time, surfacing two pre-existing defects (a UTF-8 BOM and a machine-local `file:///` link) that were fixed in-branch, plus an `empty Phase Summary` WARN that moved 15 -> 17 and was deliberately ACCEPTED — 15 peer logs share it, so fixing only these two would be inconsistent. A planned fourth item (removing `.gitkeep` placeholders) was DROPPED at `/plan` after evidence showed they are framework-deployed by `deploy.sh:901-905`. Out of scope: Tier 2 (5 diverged duplicate pairs) and Tier 3 (17 drifted docs between `docs/` and `.agentcortex/docs/`), both queued as follow-up batches.
- Tests: Pass (288 passed / 1 deselected, +5 new `tests/test_repo_hygiene.py` structural guards, each empirically falsified before acceptance; `validate.sh` 92 PASS / 9 WARN / 0 FAIL — counts identical to the pre-change baseline)

### Ship-feature-reproducible-reference-2026-07-02
- Feature shipped: Reproducible Reference Layer (Honest Research Workbench epic #5, **final feature — epic complete**). Packages the end-to-end `data→label→train→backtest→eval` pipeline into a one-command map + walkthrough for the developer/learner audience; reuses existing tools, adds no app runtime/ML/data change. `scripts/reproduce_pipeline.py` is a pure injectable orchestrator: the default prints the pipeline MAP (5 stages, each with its real code + run command) with **zero side effects and zero heavy imports** (all heavy imports are lazy inside stage fns); `--run` auto-runs only the fast/robust stages in-process (data seed → `get_history_context`; net-of-cost `run_time_machine` backtest) and marks label/train/eval **`auto_ran:False` with their real commands rather than faking them** (train is heavy + degenerate on demo); a failing stage is recorded `{error}` and the run continues honestly. `docs/REPRODUCE.md` walks all five stages pointing at the REAL code, with the degenerate-model **honesty caveat mandatory at every surface** (module docstring, map, --run, summary, doc) and links to `ml-label-oos-evaluation.md` / `DATA_INTEGRITY.md` / `/transparency`. Independent review + tenth-man → READY; the reviewer verified EVERY doc reference/command resolves to real code and confirmed no faked stage / no missing caveat. Out of scope: auto-training in the default (heavy + degenerate — documented as manual/opt-in), a CI job running the full real pipeline.
- Tests: Pass (+4 reproduce_pipeline: canonical order, no-side-effect map default + caveat, injected-order run, honest-failure-continues; backend/scripts 171/171 non-integration; map smoke ok; 1 pre-existing live-yfinance integration test deselected)

### Ship-quickwin-strategies-csrf-parity-2026-07-02
- Quick-win shipped: Strategies CSRF-header parity (Honest Research Workbench epic #6, review-finding from the #1 Strategy Lab review). `POST/PUT/DELETE /api/strategies` now require the `X-Requested-With: XMLHttpRequest` header (403 otherwise) via a shared `_require_xrw(request)` helper — parity with the shipped `/api/smart_scan` defense (`backend/routes/stock.py:178`). No active vuln (the app has no cookie auth, so no ambient credential to ride), but it restores consistency with the `api-security-hardening` baseline. Read-only GET (list, compare) unaffected; guard runs before try/except so a 403 can't be swallowed into a 500. Frontend unchanged — `mutateJson` already sends the header (added in #1); all three FE mutations route through it. Test fixture sends the header by default (CRUD unaffected) + a new falsifiable test asserts 403 without it (independent reviewer empirically removed the guards → the test failed, proving it's not vacuous). Out of scope: full CSRF token system (unnecessary without cookie auth).
- Tests: Pass (Backend 167/167 non-integration, +1 CSRF test; frontend unaffected; 1 pre-existing live-yfinance integration test deselected)

### Ship-feature-novice-entry-2026-07-02
- Feature shipped: Novice Entry (Honest Research Workbench epic #4, onboarding/ux). A dismissible, localStorage-remembered `GettingStarted` panel at the top of the Dashboard that orients a casual "just want to invest" user in plain language before they misread a technical score as a buy tip: honest positioning (a research workbench, **NOT investment advice / not a tipster**, "不會告訴你買什麼會賺"), honest expectations (technical scores are not guarantees; AI probability may be N/A / 示範模式 by design → links to 系統透明度), guided react-router quick-links to candidates / 選股雷達 / Strategy Lab / 系統透明度, and a 示範模式 note when `model_health` is unavailable. Progressive disclosure: dismiss persists in `localStorage` (guarded → degrades to always-show if storage blocked); a "顯示新手導引" toggle re-opens it. Frontend-only; no backend/data/ML. Existing Dashboard tests wrapped in a router + collapse the guide to stay isolated. Independent review + tenth-man pre-mortem → READY (no fixes; two INFO-only notes: pre-existing no-op `animate-fade-in` convention; demo note gates unavailable per spec, degraded covered by ModelHealthBanner). Out of scope: interactive coach-marks tour, account persistence.
- Tests: Pass (Frontend 80/80, +4 GettingStarted; production build green; backend unaffected — frontend-only)

### Ship-feature-explainable-screening-2026-07-02
- Feature shipped: Explainable Screening (Honest Research Workbench epic #3, screening). Makes the candidate score panel legible and closes a real honesty gap — the AI probability was shown bare even when the active model has zero buy-signal power. `V4StockDetailService.get_stock_detail` additively includes **guarded** `model_health` (`get_model_health`) in BOTH the cached-DB (<6h) and recompute paths — a corrupt `models_history.json` degrades honestly to `{status:unavailable}` instead of 500-ing the detail endpoint (tenth-man resilience fix). `ScoreBreakdown` now qualifies the AI number by `model_health` (honest caption when degraded/unavailable, none when ok; N/A stays N/A), surfaces the already-computed fired signals (KD golden cross / volume spike / squeeze) as plain-language chips, adds a caption to each sub-score bar, and states signals are contributing factors **NOT a validated per-signal win-rate** (per-signal backtested hit-rate deferred, not faked). No new endpoint, no ML change. Independent review + tenth-man pre-mortem → READY; live-verified on real 2330 (degraded model_health surfaced next to AI 1.2%). Out of scope: per-signal backtested hit-rate, model retraining.
- Tests: Pass (Backend 166/166 non-integration, +3 detail model_health both-paths + resilience; Frontend 76/76, +5 ScoreBreakdown; production build green; 1 pre-existing live-yfinance integration test deselected)

### Ship-feature-transparency-panel-2026-07-02
- Feature shipped: Transparency Panel (Honest Research Workbench epic #2, transparency). A first-class READ-ONLY view that makes the project's differentiator visible — `GET /api/transparency` aggregates existing honest primitives (`report_history_coverage` + `get_history_context` for data coverage; `get_model_health` + the active `models_history.json` entry for model state + `oos_metrics`) into one payload; **counts only** (never the `short` ticker list), **honest nulls** when data/model absent (never fabricated), server-side ~60s TTL cache (thread-safe, recompute-under-lock so concurrent cold hits don't each rescan) + `10/min` rate-limit, no `str(exc)` leak. Frontend `/transparency` page + "系統透明度" nav entry with progressive disclosure: a novice headline (data coverage + demo-mode/degraded/trained status chip with the honest `model_health` message) always visible; expert OOS metrics + coverage breakdown + train-label class-distribution behind an expand toggle, each with plain-language captions and an explicit "OOS, not a forward guarantee" label. Process: backend delegated to a Sonnet subagent; independent Opus review + tenth-man pre-mortem → READY (added a test pinning the real `timestamp`-only history schema so the `trained_at` fallback can't silently regress; surfaced the previously-unrendered class_distribution). **Live-verified on the real 92-ticker DB**: universe 1819, honest degraded-model disclosure (accuracy 0.94 inflated by 90.7% Hold while strong/buy precision+recall == 0.0), no short-list leak, cache ~4ms. Out of scope: new data/ML, per-ticker coverage drill-down, changing the consumed primitives.
- Tests: Pass (Backend 163/163 non-integration, +7 transparency; Frontend 71/71, +5; production build green; 1 pre-existing live-yfinance integration test deselected)

### Ship-feature-strategy-lab-2026-07-02
- Feature shipped: Strategy Lab (Honest Research Workbench epic #1, flagship, backtest). Adds a persistence + comparison layer ON TOP of the existing backtest engine WITHOUT changing backtest math or ML: new SQLite `strategies` table (`init_db`, `NOT NULL UNIQUE`, no DROP; survives seed/recalc — guard-tested), `StrategyRepository` (parameterized CRUD + idempotent preset seed), `backend/routes/strategies.py` with a Pydantic `StrategyParams` mirroring `/api/backtest`'s exact bounds (rejects NaN/Inf/missing/extra keys, no silent coercion), CRUD + a throttled (`5/min`) compare API (server-side ids validation, per-id timeout → honest generic error slot, summary-only, no `str(exc)` leak). Frontend Strategy Lab on `/backtest` (progressive disclosure: suggested "Balanced" starting point + inline plain-language metric interpretations for novices; save/rename/delete + select-to-compare for experts), `StrategyCompare` with honesty guards (past≠future/noise disclaimer, sample-size + date-range data-sufficiency context, NO comparative cross-column coloring, honest "回測失敗" on error). Also fixed a real NaN/Inf→500 leak via a project-wide `RequestValidationError` handler (verified no regression). Process: spec reviewed by a 3-agent panel pre-implementation (v1.0.0→v1.1.0); backend delegated to a Sonnet subagent; independent Opus review READY with all med/low findings fixed in-branch (rename UI, error-message extraction, executor `cancel_futures`, negative-id guard). Deferred: CSRF-header parity → backlog #6 (review-finding). Out of scope: backtest math/ML changes, portfolio optimization, live trading, cloud sync.
- Tests: Pass (Backend 155/155 non-integration, +40 strategy tests; Frontend 66/66, +9; production build green; 1 pre-existing live-yfinance integration test deselected — unrelated)

### Ship-feature-docker-self-seed-2026-06-20
- Feature shipped: Docker self-seed + .dockerignore (#6 of Directly-Usable v1, P1, infra — final feature; **epic complete**). `docker compose up` / `docker run` now yield a POPULATED dashboard instead of an empty one. `Dockerfile` COPYs `scripts/` + `data/demo/` and runs a new `docker-entrypoint.sh` that idempotently seeds the offline demo before `exec`ing the server; new `.dockerignore` excludes the dev `storage.db` (root + `data/` variants), `*.pkl` models, `node_modules`, built `dist`, `.git`/governance dirs (big context cut; nothing dev/secret baked). Honesty: no model baked → AI stays N/A in the container; demo data is the real #2 fixture. Docker not live-built (no daemon in env/CI) — verified by a guard test (COPY + entrypoint + .dockerignore contract) + the offline seed (15 tickers, 15 scores, AI NULL) + inspection; CRLF handled via `sed -i`. Independent review caught a real HIGH (root `storage.db` initially un-excluded → would bake an 11 MB dev DB) — fixed in-branch (root-anchored excludes + tightened test); re-review READY. Out of scope: building/publishing an image in CI; hosted deployment.
- Tests: Pass (Backend 227/227, +4 docker guard tests; full suite green, 1 integration deselected; offline container-first-boot seed verified)

### Ship-feature-real-ai-first-run-2026-06-20
- Feature shipped: Opt-in real-AI first-run (#5 of Directly-Usable v1, P1, ml). `scripts/setup_real_ai.py [--days 1100] [--listed-only] [--min-tickers 50]` chains the fast bulk backfill (#4) → coverage gate → train (`backend.train_ai.main`) → recalc (`recalculate_all(incremental=False)`) so AI probabilities are populated instead of N/A. N/A stays the default (no model bundled; `.pkl` gitignored). **Honest gating**: if fewer than `--min-tickers` (50) tickers have ≥ MIN_TRAIN_ROWS history, training is SKIPPED and AI stays N/A (a tiny-data model is degenerate — worse than honest N/A); below the ~92 OOS floor it trains but warns; the resulting weak model is disclosed by `model_health` (shipped #3). The four steps are injectable (4 unit tests, no net/DB/train). Reuses shipped `train_ai`/`recalculate` UNCHANGED (additive, opt-in, never auto-run). **Honest finding from a bounded real run**: a non-degenerate model needs ~1000+ rows/ticker (~4y) — a 320-row train yielded only 51 samples / 100% Hold; hence the `--days 1100` default + documented per-ticker-history need. Chain payoff PROVEN on the real local OOS dataset (92 tickers, 1099 rows/ticker, real model → 11 score rows all with non-NULL ai_probability, honestly weak 0.02–0.33). Independent review READY (2 LOW + 1 NIT fixed in-branch). Out of scope: model quality / new architecture; bundling a model; Docker (#6).
- Tests: Pass (Backend 222/222, +4 setup_real_ai tests incl. honest-skip + weak-model-warning; full suite green, 1 integration deselected; chain payoff verified on real data)

### Ship-feature-accelerated-universe-sync-2026-06-20
- Feature shipped: Accelerated full-universe history sync (#4 of Directly-Usable v1, P1, data) — the user-chosen "option C": replace the slow per-stock yfinance crawl (~10–15 min) with authoritative TWSE/TPEX **per-day bulk** endpoints. `core/bulk_history.py` parses TWSE `MI_INDEX?type=ALLBUT0999` (JSON, ~1,366 listed/day) + TPEX daily-close (CSV via `csv.reader` for quoted-comma volumes, ~889 OTC/day) — each call returns ALL stocks' OHLCV for a trading day; `backfill_bulk` assembles per-ticker and persists via `save_to_db`; `scripts/fast_backfill.py --days N [--listed-only]`. Parsers are pure + HTTP-injectable (no live calls in CI). The yfinance per-stock path is untouched (fallback). Prices are raw (not split/dividend-adjusted) — documented (a bulk DB is internally consistent). Validated on LIVE data (TWSE 1219 + TPEX 889 rows for 2026-06-17; 2330/0050/6488 OHLCV correct) and end-to-end (`--days 2` persisted 2,108 tickers, report == DB). Independent review READY; fixed MEDIUM (`save_to_db` now returns rows written; `backfill_bulk` counts confirmed saves + `save_failures` so the report can't over-state persistence) + LOW (skip zero-row decoy tables) in-branch. Out of scope: wiring bulk into default `/api/sync`; training (#5); Docker (#6).
- Tests: Pass (Backend 219/219, +7 bulk_history tests incl. live-validated parsers, holiday/empty, quoted-comma volume, save-failure honesty, decoy-table; full suite green, 1 integration deselected)

### Ship-feature-honest-first-run-ux-2026-06-20
- Feature shipped: Legible honest first-run UX (#3 of Directly-Usable v1, P1, UI/design-gated). Made the honest no-model/demo state read as an intentional demo instead of "broken": Traditional-Chinese sidebar nav (儀表板/AI 回測/市場風險/選股雷達) + SniperCard/StockList empty/loading/error states + backend AI Analyst (`v4_stock_detail_service`: 6 trend/RSI/signal phrases + neutral fallback, both code paths); a "示範模式 · AI 未訓練" demo badge in the dashboard header keyed off the existing `model_health.status==='unavailable'`; AI "N/A" reframed inline with a `Tooltip` ("尚未訓練 AI 模型…" / "尚未訓練模型") in `ScoreBreakdown` + `CandidateRow` (value stays honestly absent — never a fake number); the full-market sync button now gated behind a `window.confirm` warning of the ~10–15 min run. Reused the existing dark glassmorphism design system + `Tooltip` (no new visual language); §4.4 design-gate satisfied via documented deltas + a real-server Playwright screenshot. Independent fresh-agent adversarial review READY; one MEDIUM (badge + sync-confirm had no test) closed in-branch with a new `Dashboard.test.tsx` (4 tests). Honesty-first preserved (no data/model/scoring change). Out of scope: data-sync acceleration (#4), real-AI first-run (#5), Docker (#6), full app i18n.
- Tests: Pass (Backend 212/212; Frontend 57/57, +4 Dashboard badge/sync tests; full suite green, 1 integration deselected; build ✓; e2e demo-state screenshot verified)

### Ship-quickwin-credible-demo-dataset-2026-06-20
- Quick-win shipped: Credible demo dataset (#2 of Directly-Usable v1). Replaced the 6 obscure demo tickers (1240/1259/1264/1268/1336 + 2330) with 15 recognizable large-cap TWSE names (2330 台積電, 2317 鴻海, 2454 聯發科, 2412 中華電, 2882 國泰金, 2308 台達電, 2303 聯電, 2881 富邦金, 1301 台塑, 2002 中鋼, 2603 長榮, 3008 大立光, 2891 中信金, 2886 兆豐金, 1216 統一) so the first-run "Top Candidates" looks credible instead of broken. Added reproducible maintainer tool `scripts/gen_demo_fixture.py` that fetches REAL auto-adjusted prices via yfinance and rewrites `data/demo/demo_prices.csv` (15 tickers × 485 rows, 2024-06→2026-06, 362 KB); the demo runtime (`seed_demo.py`) stays fully offline. Honesty preserved (real prices, never synthesized; AI still NULL without a model). EXTENDS onboarding-quickstart.
- Tests: Pass (Backend 212/212, +1 credible-set test; offline seed verified → 15 score rows, top picks 2454/1216/2303; full suite green, 1 integration deselected)

### Ship-feature-onboarding-single-command-launch-2026-06-20
- Feature shipped: One-command launch + single-port served frontend (#1 of Directly-Usable v1, P0). A fresh clone was forced into a two-terminal, two-port setup (Vite dev :5173 + backend :8000) because `quickstart` never built the frontend, so the backend's existing `frontend/v4/dist` serving path 404'd. Now `quickstart.ps1`/`.sh` run `npm ci && npm run build` (graceful if Node absent); the backend serves the built SPA at a single URL (http://localhost:8000) via the existing `read_index` plus a new `spa_fallback` catch-all (`backend/main.py`) returning index.html for react-router deep links (/backtest, /risk, /indicators) while excluding `/api`, `/assets`, `/static` (bare + prefixed) so it never shadows them. `start.bat`/`start.sh` repurposed to single-port launch; new `start.ps1` for PowerShell parity (maintainer is on PowerShell); README reordered (offline single-URL primary, full yfinance sync demoted to optional/slow); the two-terminal dev workflow (`npm run dev`) is preserved and documented. Independent fresh-agent adversarial review READY (2 LOW findings fixed in-branch: bare reserved-segment 404, start.ps1 parity). Honesty-first unchanged (AI stays N/A; no data/model behavior touched). Out of scope: Docker self-seed (#6), demo-data swap (#2), UX legibility (#3), data-sync acceleration (#4), real-AI first-run (#5).
- Tests: Pass (Backend 211/211, +6 new SPA serving tests; full suite green, 1 integration deselected; frontend `npm run build` ✓; e2e single-port :8000 serving + fallback + API-not-shadowed verified)

### Ship-ml-label-oos-evaluation-2026-06-13
- Shipped: honest follow-up verification of #2 (ATR labels). Expanded storage.db from 6 → 92 liquid TWSE tickers (~99k rows) and ran an OOS comparison (production ensemble, chronological 80/20 split) of `fixed` vs `atr` labels. **Honest findings** (docs/specs/ml-label-oos-evaluation.md + reproducible `scripts/eval_label_modes.py`): the degenerate model (precision=recall=0) did NOT reproduce at 92-ticker scale under EITHER mode — it was largely a small-data/low-volatility artifact of the 6-ticker dev DB; more data is the bigger lever. `atr` is a modest, directional improvement on the high-value StrongBuy class (precision 0.339→0.363, recall 0.424→0.518) and Buy precision (0.082→0.120); Buy recall slightly lower; absolute precision remains low (0.12–0.36). Accuracy (fixed 0.485 > atr 0.351) is not comparable (fixed's 77% Hold inflates it). Caveat: 92 tickers ≈ 5% of universe, large/liquid-cap biased — NOT full-universe. Recommendation: keep LABEL_MODE=atr; the larger future lever is universe coverage. No code-behavior change; no model committed (.pkl gitignored).
- Tests: N/A (evaluation + docs; no app code changed). Full suite unaffected (205 pass).

### Ship-feature-onboarding-quickstart-2026-06-13
- Feature shipped: Frictionless onboarding (#1 of Optimization Round 2, P1 — epic complete) — PR #24 (stacked on #23/#22). A fresh clone was empty (`storage.db` + model gitignored) until a slow yfinance sync. Bundled `data/demo/demo_prices.csv` (6 TWSE/OTC tickers incl. 2330) + idempotent offline `scripts/seed_demo.py` (loads fixture, then `recalculate_all(incremental=False)` scores DB tickers — no network) + `quickstart.sh`/`quickstart.ps1` (install→seed→run) + README "⚡ Quickstart" section. README strategy/label description corrected to the shipped ATR-default labeling (docs match code). **Honesty bug fix** in `backend/recalculate.py`: V4 technical scoring was gated behind a loadable model, so a no-model fresh clone produced 0 scores (empty dashboard) — contradicting the feature's promise; now technical scores compute regardless of model and AI probability stays NULL (honest N/A). Verified real fresh-clone (no model) → 6 score rows, AI NULL. Out of scope: bundling a trained model, production deployment, FE build automation.
- Tests: Pass (Backend 205/205, +4 new; full suite green; 1 integration deselected)

### Ship-feature-ml-label-redesign-2026-06-13
- Feature shipped: ML label redesign (#2 of Optimization Round 2, P0) — PR #23 (stacked on #22). Replaced the fixed-percentage triple-barrier training labels (+15%/+10%/-5% over 20d) — which ignore per-stock volatility and produced the degenerate class distribution (dev data Hold 91.1% / Buy 4.9% / Strong 4.0%, low-vol stocks ~100% Hold) behind the Buy/StrongBuy precision=recall=0 finding — with **ATR volatility-scaled** barriers. `core/config.py` + `core/ai/common.py`: `LABEL_MODE` (default `atr`), `ATR_TARGET_MULT`/`ATR_BUY_MULT`/`ATR_STOP_MULT` (config = SSoT). `core/ai/trainer.py`: extracted `_compute_targets()` branching on mode; `atr` scales barriers by per-row ATR-14 at entry (no look-ahead), `fixed` reproduces legacy labels byte-for-byte (toggle to revert); NaN-ATR warm-up → Hold then dropped. `core/ai/label_analysis.py` (new): reproducible distribution-evidence tool. Independent fresh-agent review READY + honesty audit clean (no overclaim). **HONEST SCOPE**: validates label-distribution rebalancing only (91/5/4 → 59.5/13.7/26.8 via shipped tool); full-universe OOS precision/recall NOT claimed (dev DB = 6 tickers); existing saved models + live predictions unchanged; new labels affect only the next training run. Out of scope: backtest exit rules, model architecture, retrain.
- Tests: Pass (Backend 201/201, +11 new; full suite green; 1 integration deselected)

### Ship-feature-data-universe-completeness-2026-06-13
- Feature shipped: Listed/OTC universe & price-data completeness (#3 of Optimization Round 2) — PR #22. Replaced the stale `twstock` static list as the primary universe source with authoritative live TWSE STOCK_DAY_ALL (上市) + TPEX daily-close (上櫃) endpoints, promoted into a reusable `core/universe_source.py` (de-duplicating `fetch_stocks.py`). ETFs (00-prefixed, 4–6 digits) now included and tagged `kind`; warrants/rights excluded. `get_all_tw_stocks` rewritten as a 5-tier fallback chain (memory→fresh-file→live→twstock→stale) that never blanks a good cache on an empty fetch; entries gain additive `market`/`kind` keys (legacy caches load via `_normalize_loaded`). Added `refresh_stock_universe(force)`, `report_history_coverage()` (predict/train coverage vs MIN_PREDICT_ROWS/MIN_TRAIN_ROWS), `backfill_history()`. Independent fresh-agent adversarial review READY; 2 LOW latent defects found & fixed in-branch (None→"None" phantom ticker guard; TPEX volume column). Out of scope: ML training-set semantics (#2), intraday data, DB schema.
- Tests: Pass (Backend 190/190, +15 new; full suite green; 1 integration deselected)

### Ship-honesty-first-2026-06-13
- Feature shipped: Honesty-first epic (data consistent with claims) — 6 changes merged via PR #20. (1) Frontend: removed `mockData.ts` fallback, nullable `useCachedApi`, honest loading/no-data/connection-error states (MarketRisk dead guard fixed). (2) Backend: `predict_prob` returns None on all failures (incl. exception, was `{"prob":0.0}`); `ai_prob` stored as NULL not fake 0.0; `to_ai_percent` helper; readers emit null; frontend "N/A"/"資料不足". (3) `get_model_health` + `/api/market_status.model_health` + `ModelHealthBanner` warns when active model is degraded (buy/strong precision=recall=0) or unavailable. (4) Backtest `profit_factor`/`net_profit_factor` None not 9999 sentinel; "Sharpe" relabelled unannualized single-period; stop/target tooltips follow sliders. (5) Docs synced (API_CONTRACT `/api/sync` + backtest V4.2 params + model_health, README stock-count/sync/workers, ARCHITECTURE lock, TESTING). (6) Frontend CI workflow (vitest + production build) enforces the frontend gate. Out of scope: model retraining, annualized Sharpe.
- Tests: Pass (Backend 176/176 +12, Frontend 53/53 +9, Production build ✅, PR #20 CI green)

### Ship-backtest-and-performance-opt-2026-06-13
- Feature shipped: Backtest hardening + candidate list performance optimization (merged via PR #7). Backend: Taiwan-standard transaction friction — configurable `commission_rate`/`tax_rate`/`slippage_rate` on `/api/backtest` applied to Net ROI & Net Win Rate; Sharpe Ratio (mean net return / stddev) and Worst Drawdown (worst single-stock MDD) added to backtest summary; Net Profit Factor; configurable strategy parameters (`target_gain`/`stop_loss`/`holding_days`) exposed at API and wired to UI sliders. Frontend: candidate list virtualization + bulk sparkline preload via `/api/v4/meta` (eliminates per-row HTTP waterfall on dashboard render); collapsible transaction-cost panel + expanded metrics on Backtest page; `core/data.py:load_sparklines_from_db` single-query batched sparkline retrieval.
- Tests: Pass (Backend 167/167, Frontend 44/44, Production build `tsc -b && vite build` ✅)

### Ship-gemini-audit-hardening-2026-06-02
- Feature shipped: System hardening based on audit findings. Fixed GET API write side-effect in `GET /api/market_status` by moving history saving to background sync task. Added 0.5s-1.5s random sleep to yfinance download loop to prevent rate-limiting. Fixed US/Crypto stock suffix mapping bug in `get_ticker_suffix`. Integrated background sync trigger and real-time progress bar UI into the frontend dashboard. Added rate-limiting (5/minute) to `POST /api/sync`.
- Tests: Pass (Backend 165/165, Frontend 44/44, Production build ✅)

### Ship-claude-frontend-ai-opt-2026-06-01
- Feature shipped: Enhanced frontend visual aesthetics & scroll performance (SVG Sparklines & smooth transitions), verified AI training pipeline correctness (dynamic labels check), integrated TPEX/OTC stocks with suffix fallback logic, protected model activation via atomic file replacement, and updated system documentation.
- Tests: Pass (Backend 163/163, Frontend 44/44, Production build ✅)

> _Older Ship History entries (2026-03-18 … 2026-03-28) archived to `.agentcortex/context/archive/ship-history-2026.md`._
