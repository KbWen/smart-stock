# Work Log: chore/repo-hygiene-tier3

## Header

- Branch: `chore/repo-hygiene-tier3`
- Classification: `feature`
- Classified by: `claude-opus-5`
- Frozen: `true`
- Created Date: `2026-07-26`
- Owner: `KbWen`
- Guardrails Mode: `Full`
- Current Phase: `ship`
- Diff Base SHA: `f15668ec31ddffa3b143a385ac1d1bd6747840f4`
- Checkpoint SHA: `d532a18`
- Recommended Skills: `verification-before-completion (auto), red-team-adversarial (auto, Full), karpathy-principles (auto), systematic-debugging (auto, on-encounter)`
- Primary Domain Snapshot: `none`
- SSoT Sequence: `2`

---

## Session Info

- Agent: `claude-opus-5` · Session: `2026-07-26 14:00 UTC` · Platform: `claude-code`
- Guardrails loaded: AGENTS.md §Core Directives, state_machine.md, security_guardrails.md, shared-contracts.md
- Override / Downstream-Capabilities / private: absent
- Read receipt: SSoT known this session (Update Sequence 2 after the Tier 2 ship) · Work Log created ·
  Spec: `docs/specs/retire-legacy-docs-layout.md` (authored this phase)

---

## Task Description

Tier 3 of 3 — retire the legacy `docs/` framework-documentation layout. Tiers 1 (#56) and 2 (#57) merged.

Delete 17 stale framework-doc copies from `docs/` that duplicate `.agentcortex/docs/`, and reconcile the
half-applied `.gitignore` "Template-provided Documentation" block. Full problem statement, evidence table,
AC-1…AC-6 and non-goals: `docs/specs/retire-legacy-docs-layout.md`.

**Classification rationale**: the `bootstrap.md §0` table sets no floor here (no `docs/specs/` content
change, no installer surface, no governance file, nothing frozen). Size does: **17 files / 1631 deleted
lines** trips the `state_machine.md` Scope-Escalation hard block (`> 200 lines`), which makes escalation
above `quick-win` mandatory. `feature` was chosen over `hotfix` because the durable value of this batch is
the *retirement record* — which files, why, and the evidence — which is exactly what a spec is for.

Chain: `/spec` (done) → `/plan` → `/implement` → `/review` → `/test` → `/handoff` → `/ship`.

---

## Phase Sequence

| Phase | Status | Entered | Notes |
|---|---|---|---|
| bootstrap | done | 2026-07-26 | `feature` via size escalation (1631 lines) |
| spec | done | 2026-07-26 | `docs/specs/retire-legacy-docs-layout.md`, 6 ACs |
| plan | done | 2026-07-26 | 20 paths; Confidence 95% |
| implement | done | 2026-07-26 | 17 deletions + spec + guard + .gitignore |
| review | done | 2026-07-26 | PASS — 6/6 AC proven, 0 blocking |
| test | done | 2026-07-26 | 289 passed; guard falsified |
| handoff | done | 2026-07-26 | see ## Resume |
| ship | done | 2026-07-26 | SSoT Seq 2→3; archived |

---

## Phase Summary

- bootstrap: classified `feature`. The notable outcome is a **third consecutive scope correction**: I had
  told the user Tier 3 would need an ADR for a doc-topology decision. The evidence says there is no
  decision to make — `deploy.sh`, the manifest and both validators already treat `.agentcortex/docs/` as
  canonical, and `deploy.sh:1161-1178` already de-registers 14 of the 17 paths. What remains is deletion
  of artifacts the framework itself abandoned.
- plan/implement: 17 deletions (list generated by set-intersection, never hand-typed) + spec + namespace
  guard + `.gitignore` reconciliation. | Confidence: 95% — high.
- review: PASS. All 6 ACs PROVEN, 0 blocking findings. Red Team Full found no attack surface — the change
  removes documentation only, adds no code path, and touches nothing the framework reads.
- test: 289 passed / 1 deselected (+1 namespace guard, falsified by re-adding an overlapping file).
- ship: PASS. validate output **byte-identical** to a stash-clean baseline — the strongest possible
  evidence that all 17 files were pure orphans.

⚡ ACX

---

## Gate Evidence

- Gate: bootstrap | Verdict: PASS | Classification: feature | Timestamp: 2026-07-26T14:00:00Z
- Gate: plan | Verdict: PASS | Classification: feature | Timestamp: 2026-07-26T14:20:00Z
- Gate: implement | Verdict: PASS | Classification: feature | Timestamp: 2026-07-26T14:35:00Z
- Gate: review | Verdict: PASS | Classification: feature | Timestamp: 2026-07-26T14:45:00Z
- Gate: test | Verdict: PASS | Classification: feature | Timestamp: 2026-07-26T14:50:00Z
- Gate: handoff | Verdict: PASS | Classification: feature | Timestamp: 2026-07-26T14:55:00Z
- Gate: ship | Verdict: PASS | Classification: feature | Timestamp: 2026-07-26T15:00:00Z

---

## External References

| Type | Path | Notes |
|---|---|---|
| Spec | `docs/specs/retire-legacy-docs-layout.md` | 6 ACs; `status: draft` until ship |
| Prior | PR #56 / `2c97e05` | Tier 1 |
| Prior | PR #57 / `f15668e` | Tier 2 |
| Follow-up | `.github/workflows/integrity-check.yml:62-64` | Comment still calls `docs/CODEX_PLATFORM_GUIDE.md` framework-managed — stale under the current layout. Out of scope. |

---

## Known Risk

- **R1 (medium) — a deleted file might still be reachable.** Mitigation already gathered: no project-owned
  file references any of the 17; the only mentions are `CHANGELOG.md:71` (historical release note) and an
  archived 2026-02 work log. Plan must re-verify with an anchored grep — the naive pattern `docs/<name>`
  matches `.agentcortex/docs/<name>` as a substring and produced false positives on the first attempt.
- **R2 (medium) — a validator might require a bare `docs/` copy.** Checked: `validate.sh:693,723-724,
  1112-1121` and `validate.ps1:680,712-713,1070-1081` resolve every framework-doc check to
  `.agentcortex/docs/`. The two `docs/README_zh-TW.md` references are `if [[ -f ]]`-guarded and that file
  does not exist here (removed in #47).
- **R3 (low) — deleting 1631 lines could mask an accidental removal of project content.** Mitigation: the
  delete list is generated by set-intersection with `.agentcortex/docs/`, never hand-typed, and AC-2
  asserts what remains.
- **R4 (accepted) — four untracked English copies stay on local disks.** Removing untracked state is not
  git-revertible; the repository-visible outcome is identical either way. Recorded as a spec non-goal.
- **Rollback**: single squashable commit on a dedicated branch; `git revert <sha>` restores all 17 files.
  No untracked or gitignored state is destroyed, so the revert is complete.

---

## Decisions

See `docs/specs/retire-legacy-docs-layout.md §Domain Decisions` — 1 `[DECISION]`, 1 `[CONSTRAINT]`,
1 `[TRADEOFF]`.

---

## Conflict Resolution

none — `karpathy-principles` vs `verification-before-completion` is `compatible`; no other recommended
pair appears in the matrix.

---

## Skill Notes

- **karpathy-principles**: §1 surface assumptions. Responsible for all three scope corrections this session.
- **verification-before-completion**: 5-gate sequence at implement / test / ship.
- **red-team-adversarial**: Full mode at `/review` and adversarial cases at `/test` (feature row).
- **systematic-debugging**: on encounter.

---

## Drift Log

- Skip Attempt: NO · Gate Fail Reason: N/A · Token Leak: NO
- **Third scope correction of the session.** After Tier 2 I told the user Tier 3 "likely warrants an ADR"
  because it changes doc topology. It does not: the topology decision is already implemented by
  `deploy.sh`, the manifest and both validators, and the framework's own `managed[]` block de-registers
  the paths. The batch is deletion of abandoned artifacts, not a design choice. Size — not ambiguity — is
  what pushes it above `quick-win`.
- **Grep methodology error, caught in-phase.** The first inbound-reference scan used the pattern
  `docs/<name>`, which matches `.agentcortex/docs/<name>` as a substring, and reported the manifest and
  `AGENTS.md` as referencing the stale copies. Re-run with an anchored pattern excluding
  `.agentcortex/docs/`, the true answer is zero project-owned references. Lesson: when two paths are
  prefixes of one another, an unanchored grep will manufacture false dependencies.
- ADR coverage: `check_adr_coverage.py` is a `feature`-tier check here. All four ADRs still lack
  `applies_to:` frontmatter (spawned as separate work earlier this session), so coverage matching is inert
  repo-wide and cannot report a covering ADR. Recorded rather than treated as a blocker.
- Work Log discipline: LF-only writes, compact from the start (Tier 1 tripped the 12 KB compaction FAIL
  three times).
- **Invalid gate receipt, self-inflicted and caught by the baseline run**: I wrote a
  `- Gate: spec | Verdict: PASS` receipt. `spec` is not a gate phase — validate.sh's progression checker
  accepts only bootstrap/plan/implement/review/test/handoff/ship, so the receipt registered as
  `[FAIL] work logs with illegal gate phase progression: 1`. Removed; the spec artifact is recorded in
  `## External References` and the Phase Sequence table, which is the correct place. Lesson: a phase that
  produces an artifact is not automatically a phase that emits a gate receipt.

---

## Review Feedback

none

---

## Red Team Findings

Full mode (feature). **No findings.** The change deletes documentation and adds a test; it introduces no
input, no endpoint, no trust boundary, and no code path. The one thing worth checking adversarially —
whether removing a file the framework silently reads could degrade a validator — was falsified directly:
`validate.sh` output is byte-identical before and after, and both validators were read to confirm they
resolve framework-doc checks to `.agentcortex/docs/` exclusively.

---

## Design Reference

none — no user-visible UI (Design Gate exempt).

---

## Observability

none — no error-handling code in scope.

---

## Resume

- **State**: SHIPPED. Branch `chore/repo-hygiene-tier3`, commit `d532a18`, all 7 gates PASS.
- **Completed**: all 6 ACs of `docs/specs/retire-legacy-docs-layout.md`.
- **Next**: nothing outstanding for this batch. The 3-tier hygiene programme is complete.
- **Context**: this was Tier 3 of 3; Tiers 1 (#56) and 2 (#57) merged earlier the same day.

### Read Map
- `docs/specs/retire-legacy-docs-layout.md` — problem, evidence table, AC-1…AC-6, non-goals
- `tests/test_repo_hygiene.py` — the 6 guards pinning all three tiers' decisions
- `.agentcortex/context/archive/chore-repo-hygiene-tier{1,2}-20260726.md` — prior batches

### Skip List
- `.agentcortex/docs/**` — untouched by this batch; canonical framework docs
- `docs/specs/*`, `docs/adr/*` — explicit spec non-goals

### Context Snapshot
`docs/` is now exclusively project-owned; `.agentcortex/docs/` is exclusively framework-owned; a test
enforces the split. Four untracked English framework docs remain on existing local clones by design —
they are gitignored and invisible to the repository (spec non-goal + `## Known Risk` R4).

---

## Test Gate Results

- Command: `python -m pytest tests -m "not integration" -q` → **289 passed, 1 deselected** (was 288).
- Test Files: `tests/test_repo_hygiene.py` (+1 test, now 6).
- Falsification: copying `.agentcortex/docs/NONLINEAR_SCENARIOS.md` back into `docs/` and staging it made
  `test_docs_and_framework_docs_namespaces_stay_disjoint` FAIL as required, then cleanup restored green.

---

## Evidence

- **AC-1 PROVEN**: set-intersection of tracked `docs/` and `.agentcortex/docs/` basenames is empty.
- **AC-2 PROVEN**: what remains under `docs/` is API_CONTRACT, ARCHITECTURE, CONFIGURATION,
  DATA_DICTIONARY, DATA_INTEGRITY, GLOSSARY, MODULE_GUIDE, REPRODUCE, WORKFLOW, style-guide, 5 project
  guides, project_meta/, screenshots/, adr/, specs/ — all project-owned.
- **AC-3 PROVEN**: `validate.sh` 92 PASS / 9 WARN / 0 FAIL, output **byte-identical** to the stash-clean
  baseline. No delta to explain.
- **AC-4 PROVEN**: 289 passed / 1 deselected (288 before + the new guard); no code touched.
- **AC-5 PROVEN**: guard added and falsified (see Test Gate Results).
- **AC-6 PROVEN**: `.gitignore:48-56` block re-headed to state the retirement and its purpose.
- **Security** (§1 A01–A03 + §3): no executable code paths (17 doc deletions, 1 spec, 1 test, 1 ignore
  comment). No credentials, keys, tokens, or connection strings. §4: no dependency manifest changed.
  **Findings: none.**
- Scoping evidence (full table in the spec): deploy targets `.agentcortex/docs/` only
  (`deploy.sh:998-1008`); manifest holds 30 `.agentcortex/docs/` entries and 0 bare `docs/` entries;
  `deploy.sh:1161-1178` de-registers 14 of the 17; all 17 last touched by framework-sync or hygiene
  commits; 17 files / **1631 lines** to delete.
